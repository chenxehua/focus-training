# 专注星球 - 数据库设计文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 专注星球 (FocusKids) |
| 文档类型 | 数据库设计文档 |
| 版本 | V2.0 |
| 更新日期 | 2025年5月 |
| 数据库 | MySQL 8.0 + Redis |

---

## 目录

1. [数据库概览](#1-数据库概览)
2. [数据表总览](#2-数据表总览)
3. [核心数据表设计](#3-核心数据表设计)
4. [学校管理表设计](#4-学校管理表设计)
5. [家长学院表设计](#5-家长学院表设计)
6. [索引设计](#6-索引设计)
7. [初始数据](#7-初始数据)
8. [ER关系图](#8-er关系图)

---

## 1. 数据库概览

### 1.1 技术选型

| 组件 | 选择 | 版本 | 用途 |
|------|------|------|------|
| **关系型数据库** | MySQL | 8.0 | 主数据存储 |
| **缓存数据库** | Redis | 6.0 | 会话、JWT令牌、排行榜 |
| **字符集** | utf8mb4 | - | 支持emoji和特殊字符 |

### 1.2 数据库命名

- **数据库名称**: `focuskids`
- **字符集**: `utf8mb4_unicode_ci`

### 1.3 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 表名 | 小写下划线 | `user`, `child` |
| 字段名 | 小写下划线 | `user_id`, `created_at` |
| 主键 | `id` | 自增BIGINT |
| 外键 | `{table}_id` | `child_id`, `user_id` |
| 索引前缀 | `idx_` | `idx_user_id` |
| 唯一索引前缀 | `uk_` | `uk_openid` |
| 创建时间 | `created_at` | DATETIME |
| 更新时间 | `updated_at` | DATETIME |

---

## 2. 数据表总览

### 2.1 核心业务表

| 表名 | 说明 | 主要关联 |
|------|------|----------|
| `user` | 家长用户表 | 1:N child, 1:N order |
| `child` | 儿童信息表 | N:1 user, 1:N training_record |
| `parent_child` | 家长儿童关联表 | user与child的多对多关系 |
| `game` | 游戏配置表 | 1:N training_record |
| `training_record` | 训练记录表 | 核心业务数据 |
| `achievement` | 成就定义表 | 1:N child_achievement |
| `child_achievement` | 儿童成就记录表 | child与achievement关联 |
| `membership` | 会员套餐表 | 1:N child_membership |
| `child_membership` | 儿童会员记录表 | 记录儿童的有效会员 |
| `order` | 订单表 | 支付记录 |
| `focus_report` | 专注力报告表 | 定期训练报告 |

### 2.2 学校管理表

| 表名 | 说明 | 主要关联 |
|------|------|----------|
| `school` | 学校信息表 | 1:N teacher, 1:N class |
| `teacher` | 教师表 | N:1 user, N:1 school |
| `class` | 班级表 | N:1 school, 1:N class_student |
| `class_student` | 班级学生表 | class与child的关联 |

### 2.3 家长学院表

| 表名 | 说明 | 主要关联 |
|------|------|----------|
| `academy_category` | 文章分类表 | 1:N academy_article |
| `academy_article` | 文章表 | N:1 academy_category |
| `academy_question_category` | 问答分类表 | 1:N academy_question |
| `academy_question` | 问答表 | N:1 user, N:1 academy_question_category, 1:N academy_answer |
| `academy_answer` | 回答表 | N:1 academy_question, N:1 user |

---

## 3. 核心数据表设计

### 3.1 用户表 (user)

存储家长用户信息，支持微信登录。

```sql
CREATE TABLE `user` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `openid` VARCHAR(64) NOT NULL UNIQUE COMMENT '微信openid',
  `unionid` VARCHAR(64) DEFAULT NULL COMMENT '微信unionid',
  `nickname` VARCHAR(64) DEFAULT NULL COMMENT '昵称',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `role` ENUM('parent', 'teacher', 'admin') DEFAULT 'parent' COMMENT '角色',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-禁用, 1-正常',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_openid` (`openid`),
  INDEX `idx_unionid` (`unionid`),
  INDEX `idx_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| openid | VARCHAR(64) | 微信openid，唯一，用于微信登录 |
| unionid | VARCHAR(64) | 微信unionid，可为空（未 UnionID 时） |
| nickname | VARCHAR(64) | 用户昵称 |
| avatar | VARCHAR(255) | 头像URL |
| phone | VARCHAR(20) | 手机号，可用于账号登录 |
| role | ENUM | 角色：parent-家长, teacher-教师, admin-管理员 |
| status | TINYINT | 状态：0-禁用, 1-正常 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 3.2 儿童表 (child)

存储儿童用户信息，与家长通过 `parent_child` 表关联。

```sql
CREATE TABLE `child` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `name` VARCHAR(32) NOT NULL COMMENT '儿童姓名',
  `age` TINYINT DEFAULT NULL COMMENT '年龄',
  `gender` ENUM('male', 'female', 'unknown') DEFAULT 'unknown' COMMENT '性别',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  `age_group` VARCHAR(10) DEFAULT NULL COMMENT '年龄段: 4-6, 7-9, 10-12',
  `level` INT DEFAULT 1 COMMENT '当前等级',
  `experience` INT DEFAULT 0 COMMENT '经验值',
  `privacy_consent` TINYINT DEFAULT 0 COMMENT '隐私同意: 0-未同意, 1-已同意',
  `consent_date` DATETIME DEFAULT NULL COMMENT '同意时间',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-禁用, 1-正常',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_age_group` (`age_group`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='儿童表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| name | VARCHAR(32) | 儿童姓名 |
| age | TINYINT | 年龄（4-12岁） |
| gender | ENUM | 性别：male-男, female-女, unknown-未知 |
| avatar | VARCHAR(255) | 头像URL |
| age_group | VARCHAR(10) | 年龄段分组：4-6（幼儿）、7-9（小学低年级）、10-12（小学高年级） |
| level | INT | 当前等级，初始为1 |
| experience | INT | 经验值，用于升级 |
| privacy_consent | TINYINT | 家长隐私协议同意：0-未同意, 1-已同意 |
| consent_date | DATETIME | 同意时间 |
| status | TINYINT | 状态：0-禁用, 1-正常 |

**说明**：儿童表不直接关联用户，通过 `parent_child` 关联表实现多对多关系，支持一个儿童被多个家长管理。

### 3.3 家长儿童关联表 (parent_child)

管理家长与儿童的关联关系，支持多对多。

```sql
CREATE TABLE `parent_child` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `user_id` INT UNSIGNED NOT NULL COMMENT '家长ID',
  `child_id` INT UNSIGNED NOT NULL COMMENT '儿童ID',
  `relation` VARCHAR(32) DEFAULT 'parent' COMMENT '关系: parent, guardian',
  `is_default` TINYINT DEFAULT 0 COMMENT '是否默认: 0-否, 1-是',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`child_id`) REFERENCES `child`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_user_child` (`user_id`, `child_id`),
  INDEX `idx_child_id` (`child_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='家长儿童关联表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| user_id | INT UNSIGNED | 家长用户ID |
| child_id | INT UNSIGNED | 儿童ID |
| relation | VARCHAR(32) | 关系类型：parent-父母, guardian-监护人 |
| is_default | TINYINT | 是否为默认选择的孩子：0-否, 1-是 |

### 3.4 游戏表 (game)

存储训练游戏配置信息。

```sql
CREATE TABLE `game` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `game_code` VARCHAR(32) NOT NULL UNIQUE COMMENT '游戏代码',
  `game_name` VARCHAR(64) NOT NULL COMMENT '游戏名称',
  `description` TEXT COMMENT '游戏描述',
  `icon` VARCHAR(255) DEFAULT NULL COMMENT '图标URL',
  `category` VARCHAR(32) DEFAULT NULL COMMENT '分类',
  `difficulty_levels` INT DEFAULT 5 COMMENT '难度级别数',
  `min_age` TINYINT DEFAULT 4 COMMENT '最小年龄',
  `max_age` TINYINT DEFAULT 12 COMMENT '最大年龄',
  `training_focus` VARCHAR(255) DEFAULT NULL COMMENT '训练焦点',
  `requires_vip` TINYINT DEFAULT 0 COMMENT '是否需要VIP',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-禁用, 1-正常',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_game_code` (`game_code`),
  INDEX `idx_category` (`category`),
  INDEX `idx_sort_order` (`sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='游戏表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| game_code | VARCHAR(32) | 游戏代码，唯一标识，如：schulte, audio_count |
| game_name | VARCHAR(64) | 游戏名称 |
| description | TEXT | 游戏详细描述 |
| icon | VARCHAR(255) | 游戏图标URL |
| category | VARCHAR(32) | 游戏分类：visual-视觉, auditory-听觉, memory-记忆 |
| difficulty_levels | INT | 难度级别数，默认5级 |
| min_age | TINYINT | 最小适用年龄 |
| max_age | TINYINT | 最大适用年龄 |
| training_focus | VARCHAR(255) | 训练焦点描述 |
| requires_vip | TINYINT | 是否需要VIP会员才能使用：0-免费, 1-付费 |
| sort_order | INT | 排序序号 |
| status | TINYINT | 状态：0-禁用, 1-正常 |

### 3.5 训练记录表 (training_record)

核心业务表，存储每次训练的数据。

```sql
CREATE TABLE `training_record` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `child_id` INT UNSIGNED NOT NULL COMMENT '儿童ID',
  `game_id` INT UNSIGNED NOT NULL COMMENT '游戏ID',
  `difficulty_level` TINYINT DEFAULT 1 COMMENT '难度级别',
  `score` INT DEFAULT 0 COMMENT '得分',
  `accuracy` DECIMAL(5,4) DEFAULT 0 COMMENT '准确率(0-1)',
  `focus_score` INT DEFAULT 0 COMMENT '专注度评分',
  `duration_seconds` INT DEFAULT 0 COMMENT '训练时长(秒)',
  `game_config` JSON DEFAULT NULL COMMENT '游戏配置JSON',
  `result_data` JSON DEFAULT NULL COMMENT '游戏结果JSON',
  `completed` TINYINT DEFAULT 1 COMMENT '是否完成: 0-未完成, 1-完成',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`child_id`) REFERENCES `child`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`game_id`) REFERENCES `game`(`id`) ON DELETE CASCADE,
  INDEX `idx_child_id` (`child_id`),
  INDEX `idx_game_id` (`game_id`),
  INDEX `idx_created_at` (`created_at`),
  INDEX `idx_child_game` (`child_id`, `game_id`),
  INDEX `idx_child_created` (`child_id`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='训练记录表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| child_id | INT UNSIGNED | 儿童ID，外键关联child表 |
| game_id | INT UNSIGNED | 游戏ID，外键关联game表 |
| difficulty_level | TINYINT | 训练时的难度级别 |
| score | INT | 训练得分 |
| accuracy | DECIMAL(5,4) | 准确率，范围0-1（如0.85表示85%） |
| focus_score | INT | 专注度评分，范围0-100 |
| duration_seconds | INT | 训练时长，单位：秒 |
| game_config | JSON | 游戏配置快照，记录训练时的游戏设置 |
| result_data | JSON | 详细结果数据，如反应时间、错误位置等 |
| completed | TINYINT | 是否完成训练：0-未完成, 1-完成 |
| created_at | DATETIME | 创建时间 |

**JSON字段示例：**
```json
// game_config 示例
{"grid_size": 5, "time_limit": 60, "difficulty": "hard"}

// result_data 示例
{"reaction_times": [1.2, 1.5, 1.3], "error_positions": [5, 12], "completion_time": 95}
```

### 3.6 成就表 (achievement)

存储成就定义信息。

```sql
CREATE TABLE `achievement` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `achievement_code` VARCHAR(32) NOT NULL UNIQUE COMMENT '成就代码',
  `name` VARCHAR(64) NOT NULL COMMENT '成就名称',
  `description` VARCHAR(255) DEFAULT NULL COMMENT '成就描述',
  `icon` VARCHAR(255) DEFAULT NULL COMMENT '图标URL',
  `type` VARCHAR(32) DEFAULT NULL COMMENT '类型: training, score, streak, milestone',
  `tier` ENUM('basic', 'intermediate', 'advanced', 'legendary') DEFAULT 'basic' COMMENT '等级',
  `requirement` JSON DEFAULT NULL COMMENT '解锁条件JSON',
  `reward_points` INT DEFAULT 0 COMMENT '奖励积分',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-禁用, 1-正常',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_achievement_code` (`achievement_code`),
  INDEX `idx_type` (`type`),
  INDEX `idx_tier` (`tier`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='成就表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| achievement_code | VARCHAR(32) | 成就代码，唯一标识 |
| name | VARCHAR(64) | 成就名称 |
| description | VARCHAR(255) | 成就描述 |
| icon | VARCHAR(255) | 图标URL |
| type | VARCHAR(32) | 成就类型：training-训练次数, score-得分, streak-连续天数, milestone-里程碑 |
| tier | ENUM | 成就等级：basic-基础, intermediate-中级, advanced-高级, legendary-传说 |
| requirement | JSON | 解锁条件，如：`{"type": "training_count", "value": 10}` |
| reward_points | INT | 解锁奖励积分 |
| sort_order | INT | 排序序号 |
| status | TINYINT | 状态：0-禁用, 1-正常 |

### 3.7 儿童成就解锁表 (child_achievement)

记录儿童已解锁的成就。

```sql
CREATE TABLE `child_achievement` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `child_id` INT UNSIGNED NOT NULL COMMENT '儿童ID',
  `achievement_id` INT UNSIGNED NOT NULL COMMENT '成就ID',
  `unlocked_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '解锁时间',
  `progress` INT DEFAULT 100 COMMENT '进度百分比',
  `notification_sent` TINYINT DEFAULT 0 COMMENT '通知是否已发送',
  FOREIGN KEY (`child_id`) REFERENCES `child`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`achievement_id`) REFERENCES `achievement`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_child_achievement` (`child_id`, `achievement_id`),
  INDEX `idx_child_id` (`child_id`),
  INDEX `idx_unlocked_at` (`unlocked_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='儿童成就解锁表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| child_id | INT UNSIGNED | 儿童ID |
| achievement_id | INT UNSIGNED | 成就ID |
| unlocked_at | DATETIME | 解锁时间 |
| progress | INT | 进度百分比，100表示已完成 |
| notification_sent | TINYINT | 成就解锁通知是否已发送：0-否, 1-是 |

### 3.8 会员表 (membership)

存储会员套餐定义，不是用户会员记录。

```sql
CREATE TABLE `membership` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `name` VARCHAR(64) NOT NULL COMMENT '会员名称',
  `tier` ENUM('free', 'basic', 'premium', 'vip') DEFAULT 'free' COMMENT '等级',
  `price` DECIMAL(10,2) DEFAULT 0 COMMENT '价格',
  `duration_days` INT DEFAULT 365 COMMENT '时长(天)',
  `benefits` JSON DEFAULT NULL COMMENT '权益JSON',
  `features` JSON DEFAULT NULL COMMENT '功能JSON',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-禁用, 1-正常',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_tier` (`tier`),
  INDEX `idx_price` (`price`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会员表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| name | VARCHAR(64) | 套餐名称 |
| tier | ENUM | 等级：free-免费, basic-基础, premium-高级, vip-VIP |
| price | DECIMAL(10,2) | 价格（元） |
| duration_days | INT | 有效期天数 |
| benefits | JSON | 权益列表 |
| features | JSON | 功能列表 |
| sort_order | INT | 排序 |
| status | TINYINT | 状态 |

**JSON字段示例：**
```json
// benefits 示例
["全部9个游戏", "无限训练", "完整报告", "成就系统"]

// features 示例
["all_games", "unlimited", "full_reports", "achievements"]
```

### 3.9 儿童会员表 (child_membership)

记录儿童的实际会员状态和有效期。

```sql
CREATE TABLE `child_membership` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `child_id` INT UNSIGNED NOT NULL COMMENT '儿童ID',
  `membership_id` INT UNSIGNED NOT NULL COMMENT '会员ID',
  `order_id` INT UNSIGNED DEFAULT NULL COMMENT '订单ID',
  `start_date` DATE NOT NULL COMMENT '开始日期',
  `end_date` DATE NOT NULL COMMENT '结束日期',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-过期, 1-有效',
  `auto_renew` TINYINT DEFAULT 0 COMMENT '是否自动续费',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`child_id`) REFERENCES `child`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`membership_id`) REFERENCES `membership`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`order_id`) REFERENCES `order`(`id`) ON DELETE SET NULL,
  UNIQUE KEY `uk_child_membership` (`child_id`, `membership_id`),
  INDEX `idx_child_id` (`child_id`),
  INDEX `idx_end_date` (`end_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='儿童会员表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| child_id | INT UNSIGNED | 儿童ID |
| membership_id | INT UNSIGNED | 会员套餐ID |
| order_id | INT UNSIGNED | 关联订单ID，可为空 |
| start_date | DATE | 会员开始日期 |
| end_date | DATE | 会员结束日期 |
| status | TINYINT | 状态：0-过期, 1-有效 |
| auto_renew | TINYINT | 是否自动续费：0-否, 1-是 |

### 3.10 订单表 (order)

存储支付订单记录。

```sql
CREATE TABLE `order` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `order_no` VARCHAR(64) NOT NULL UNIQUE COMMENT '订单号',
  `user_id` INT UNSIGNED NOT NULL COMMENT '用户ID',
  `child_id` INT UNSIGNED DEFAULT NULL COMMENT '儿童ID',
  `membership_id` INT UNSIGNED DEFAULT NULL COMMENT '会员ID',
  `amount` DECIMAL(10,2) NOT NULL COMMENT '金额',
  `status` ENUM('pending', 'paid', 'cancelled', 'refunded') DEFAULT 'pending' COMMENT '状态',
  `pay_method` VARCHAR(32) DEFAULT NULL COMMENT '支付方式',
  `pay_time` DATETIME DEFAULT NULL COMMENT '支付时间',
  `transaction_id` VARCHAR(128) DEFAULT NULL COMMENT '微信交易单号',
  `ext_data` JSON DEFAULT NULL COMMENT '扩展数据',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`membership_id`) REFERENCES `membership`(`id`) ON DELETE SET NULL,
  INDEX `idx_order_no` (`order_no`),
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_status` (`status`),
  INDEX `idx_pay_time` (`pay_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| order_no | VARCHAR(64) | 订单号，唯一 |
| user_id | INT UNSIGNED | 下单用户ID |
| child_id | INT UNSIGNED | 关联的儿童ID，可为空 |
| membership_id | INT UNSIGNED | 购买的会员套餐ID |
| amount | DECIMAL(10,2) | 订单金额 |
| status | ENUM | 状态：pending-待支付, paid-已支付, cancelled-已取消, refunded-已退款 |
| pay_method | VARCHAR(32) | 支付方式 |
| pay_time | DATETIME | 支付时间 |
| transaction_id | VARCHAR(128) | 微信支付交易单号 |
| ext_data | JSON | 扩展数据 |

### 3.11 专注力报告表 (focus_report)

存储定期生成的训练报告。

```sql
CREATE TABLE `focus_report` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `child_id` INT UNSIGNED NOT NULL COMMENT '儿童ID',
  `report_type` VARCHAR(32) DEFAULT 'weekly' COMMENT '报告类型: daily, weekly, monthly',
  `period_start` DATE NOT NULL COMMENT '统计周期开始',
  `period_end` DATE NOT NULL COMMENT '统计周期结束',
  `dimensions` JSON DEFAULT NULL COMMENT '7维度数据JSON',
  `overall_score` INT DEFAULT 0 COMMENT '综合评分',
  `summary` TEXT COMMENT '综合摘要',
  `recommendations` JSON DEFAULT NULL COMMENT '建议JSON',
  `training_count` INT DEFAULT 0 COMMENT '训练次数',
  `total_duration` INT DEFAULT 0 COMMENT '总时长(秒)',
  `generated_by` VARCHAR(32) DEFAULT 'system' COMMENT '生成方式: system, ai',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`child_id`) REFERENCES `child`(`id`) ON DELETE CASCADE,
  INDEX `idx_child_id` (`child_id`),
  INDEX `idx_report_type` (`report_type`),
  INDEX `idx_period` (`period_start`, `period_end`),
  INDEX `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='专注力报告表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| child_id | INT UNSIGNED | 儿童ID |
| report_type | VARCHAR(32) | 报告类型：daily-日报, weekly-周报, monthly-月报 |
| period_start | DATE | 统计周期开始日期 |
| period_end | DATE | 统计周期结束日期 |
| dimensions | JSON | 7维度评估数据 |
| overall_score | INT | 综合评分（0-100） |
| summary | TEXT | 训练总结 |
| recommendations | JSON | 训练建议 |
| training_count | INT | 统计周期内训练次数 |
| total_duration | INT | 总训练时长（秒） |
| generated_by | VARCHAR(32) | 报告生成方式：system-系统生成, ai-AI生成 |

---

## 4. 学校管理表设计

### 4.1 学校表 (school)

存储学校基本信息。

```sql
CREATE TABLE `school` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `name` VARCHAR(128) NOT NULL COMMENT '学校名称',
  `code` VARCHAR(32) UNIQUE COMMENT '学校代码',
  `province` VARCHAR(32) DEFAULT NULL COMMENT '省份',
  `city` VARCHAR(32) DEFAULT NULL COMMENT '城市',
  `district` VARCHAR(32) DEFAULT NULL COMMENT '区县',
  `address` VARCHAR(255) DEFAULT NULL COMMENT '详细地址',
  `contact_name` VARCHAR(64) DEFAULT NULL COMMENT '联系人',
  `contact_phone` VARCHAR(20) DEFAULT NULL COMMENT '联系电话',
  `contact_email` VARCHAR(64) DEFAULT NULL COMMENT '联系邮箱',
  `license_image` VARCHAR(255) DEFAULT NULL COMMENT '营业执照图片',
  `status` TINYINT DEFAULT 0 COMMENT '状态: 0-待审核, 1-已通过, 2-已拒绝',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_code` (`code`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学校表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| name | VARCHAR(128) | 学校名称 |
| code | VARCHAR(32) | 学校代码，唯一 |
| province | VARCHAR(32) | 所在省份 |
| city | VARCHAR(32) | 所在城市 |
| district | VARCHAR(32) | 所在区县 |
| address | VARCHAR(255) | 详细地址 |
| contact_name | VARCHAR(64) | 联系人姓名 |
| contact_phone | VARCHAR(20) | 联系电话 |
| contact_email | VARCHAR(64) | 联系邮箱 |
| license_image | VARCHAR(255) | 营业执照图片URL |
| status | TINYINT | 状态：0-待审核, 1-已通过, 2-已拒绝 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 4.2 教师表 (teacher)

存储教师信息，关联用户和学校。

```sql
CREATE TABLE `teacher` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `user_id` INT UNSIGNED NOT NULL COMMENT '用户ID',
  `school_id` INT UNSIGNED NOT NULL COMMENT '学校ID',
  `name` VARCHAR(64) NOT NULL COMMENT '教师姓名',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `email` VARCHAR(64) DEFAULT NULL COMMENT '邮箱',
  `role` TINYINT DEFAULT 2 COMMENT '角色: 1-管理员, 2-班主任, 3-任课教师, 4-心理老师',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-禁用, 1-正常',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`school_id`) REFERENCES `school`(`id`) ON DELETE CASCADE,
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_school_id` (`school_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| user_id | INT UNSIGNED | 关联的用户ID（教师需先注册用户） |
| school_id | INT UNSIGNED | 所属学校ID |
| name | VARCHAR(64) | 教师姓名 |
| phone | VARCHAR(20) | 手机号 |
| email | VARCHAR(64) | 邮箱 |
| role | TINYINT | 角色：1-管理员, 2-班主任, 3-任课教师, 4-心理老师 |
| status | TINYINT | 状态：0-禁用, 1-正常 |

### 4.3 班级表 (class)

存储班级信息。

```sql
CREATE TABLE `class` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `school_id` INT UNSIGNED NOT NULL COMMENT '学校ID',
  `name` VARCHAR(64) NOT NULL COMMENT '班级名称',
  `grade` VARCHAR(16) DEFAULT NULL COMMENT '年级',
  `year` INT DEFAULT NULL COMMENT '学年',
  `teacher_id` INT UNSIGNED DEFAULT NULL COMMENT '班主任ID',
  `student_count` INT DEFAULT 0 COMMENT '学生数',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-禁用, 1-正常',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`school_id`) REFERENCES `school`(`id`) ON DELETE CASCADE,
  INDEX `idx_school_id` (`school_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='班级表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| school_id | INT UNSIGNED | 所属学校ID |
| name | VARCHAR(64) | 班级名称 |
| grade | VARCHAR(16) | 年级，如"一年级"、"七年级" |
| year | INT | 学年，如2025 |
| teacher_id | INT UNSIGNED | 班主任教师ID |
| student_count | INT | 当前学生数 |
| status | TINYINT | 状态：0-禁用, 1-正常 |

### 4.4 班级学生表 (class_student)

关联班级和学生（儿童）。

```sql
CREATE TABLE `class_student` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `class_id` INT UNSIGNED NOT NULL COMMENT '班级ID',
  `child_id` INT UNSIGNED NOT NULL COMMENT '学生ID(儿童ID)',
  `student_no` VARCHAR(32) DEFAULT NULL COMMENT '学号',
  `join_date` DATE DEFAULT NULL COMMENT '加入日期',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-离班, 1-在读',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`class_id`) REFERENCES `class`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`child_id`) REFERENCES `child`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_class_child` (`class_id`, `child_id`),
  INDEX `idx_child_id` (`child_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='班级学生表';
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT UNSIGNED | 主键，自增 |
| class_id | INT UNSIGNED | 班级ID |
| child_id | INT UNSIGNED | 学生ID（关联儿童表） |
| student_no | VARCHAR(32) | 学号 |
| join_date | DATE | 加入班级日期 |
| status | TINYINT | 状态：0-离班, 1-在读 |

---

## 5. 家长学院表设计

### 5.1 文章分类表 (academy_category)

```sql
CREATE TABLE `academy_category` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `name` VARCHAR(64) NOT NULL COMMENT '分类名称',
  `name_en` VARCHAR(64) DEFAULT NULL COMMENT '分类英文名',
  `icon` VARCHAR(255) DEFAULT NULL COMMENT '分类图标',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `article_count` INT DEFAULT 0 COMMENT '文章数量',
  `is_active` TINYINT DEFAULT 1 COMMENT '是否启用: 0-禁用, 1-启用',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_sort_order` (`sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='家长学院分类表';
```

### 5.2 文章表 (academy_article)

```sql
CREATE TABLE `academy_article` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `category_id` INT UNSIGNED NOT NULL COMMENT '分类ID',
  `title` VARCHAR(128) NOT NULL COMMENT '标题',
  `cover_image` VARCHAR(255) DEFAULT NULL COMMENT '封面图',
  `summary` TEXT COMMENT '摘要',
  `content` TEXT COMMENT '内容(富文本)',
  `author` VARCHAR(64) DEFAULT NULL COMMENT '作者',
  `tags` VARCHAR(255) DEFAULT NULL COMMENT '标签,逗号分隔',
  `read_count` INT DEFAULT 0 COMMENT '阅读数',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `is_featured` TINYINT DEFAULT 0 COMMENT '是否推荐: 0-否, 1-是',
  `reading_time` INT DEFAULT 5 COMMENT '阅读时长(分钟)',
  `is_published` TINYINT DEFAULT 0 COMMENT '是否发布: 0-草稿, 1-已发布',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '是否删除: 0-否, 1-是',
  `publish_date` DATETIME DEFAULT NULL COMMENT '发布时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`category_id`) REFERENCES `academy_category`(`id`) ON DELETE CASCADE,
  INDEX `idx_category_id` (`category_id`),
  INDEX `idx_is_published` (`is_published`),
  INDEX `idx_publish_date` (`publish_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='家长学院文章表';
```

### 5.3 问答分类表 (academy_question_category)

```sql
CREATE TABLE `academy_question_category` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `name` VARCHAR(64) NOT NULL COMMENT '分类名称',
  `icon` VARCHAR(255) DEFAULT NULL COMMENT '图标',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `is_active` TINYINT DEFAULT 1 COMMENT '是否启用: 0-禁用, 1-启用',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='问答分类表';
```

### 5.4 问答表 (academy_question)

```sql
CREATE TABLE `academy_question` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `user_id` INT UNSIGNED NOT NULL COMMENT '提问用户ID',
  `category_id` INT UNSIGNED DEFAULT NULL COMMENT '分类ID',
  `title` VARCHAR(128) NOT NULL COMMENT '问题标题',
  `content` TEXT COMMENT '问题内容',
  `images` TEXT COMMENT '图片JSON数组',
  `status` TINYINT DEFAULT 0 COMMENT '状态: 0-待回答, 1-已回答, 2-已关闭',
  `view_count` INT DEFAULT 0 COMMENT '浏览数',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `answered_at` DATETIME DEFAULT NULL COMMENT '回答时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '是否删除: 0-否, 1-是',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_category_id` (`category_id`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='问答表';
```

### 5.5 回答表 (academy_answer)

```sql
CREATE TABLE `academy_answer` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `question_id` INT UNSIGNED NOT NULL COMMENT '问题ID',
  `user_id` INT UNSIGNED NOT NULL COMMENT '回答用户ID',
  `content` TEXT NOT NULL COMMENT '回答内容',
  `is_expert` TINYINT DEFAULT 0 COMMENT '是否专家回答: 0-否, 1-是',
  `is_official` TINYINT DEFAULT 0 COMMENT '是否官方回答: 0-否, 1-是',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '是否删除: 0-否, 1-是',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`question_id`) REFERENCES `academy_question`(`id`) ON DELETE CASCADE,
  INDEX `idx_question_id` (`question_id`),
  INDEX `idx_is_expert` (`is_expert`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='回答表';
```

---

## 6. 索引设计

### 6.1 主键索引

每个表都有自增主键 `id`，用于唯一标识记录。

### 6.2 外键索引

| 表名 | 外键字段 | 关联表 | 用途 |
|------|----------|--------|------|
| child | (无直接外键) | - | 通过parent_child关联 |
| parent_child | user_id | user | 关联家长 |
| parent_child | child_id | child | 关联儿童 |
| training_record | child_id | child | 关联训练儿童 |
| training_record | game_id | game | 关联训练游戏 |
| child_achievement | child_id | child | 关联成就儿童 |
| child_achievement | achievement_id | achievement | 关联成就 |
| child_membership | child_id | child | 关联会员儿童 |
| child_membership | membership_id | membership | 关联会员套餐 |
| child_membership | order_id | order | 关联订单 |
| order | user_id | user | 关联下单用户 |
| order | membership_id | membership | 关联购买套餐 |
| focus_report | child_id | child | 关联报告儿童 |
| teacher | user_id | user | 关联教师用户 |
| teacher | school_id | school | 关联所属学校 |
| class | school_id | school | 关联所属学校 |
| class_student | class_id | class | 关联班级 |
| class_student | child_id | child | 关联学生 |
| academy_article | category_id | academy_category | 关联文章分类 |
| academy_question | user_id | user | 关联提问用户 |
| academy_question | category_id | academy_question_category | 关联问题分类 |
| academy_answer | question_id | academy_question | 关联回答问题 |

### 6.3 业务索引

| 表名 | 索引名 | 字段 | 类型 | 用途 |
|------|--------|------|------|------|
| user | idx_openid | openid | UNIQUE | 微信登录 |
| user | idx_unionid | unionid | NORMAL | 微信UnionID查询 |
| user | idx_phone | phone | NORMAL | 手机号登录 |
| child | idx_age_group | age_group | NORMAL | 按年龄段查询儿童 |
| game | idx_game_code | game_code | UNIQUE | 游戏代码查询 |
| game | idx_category | category | NORMAL | 按分类查询游戏 |
| game | idx_sort_order | sort_order | NORMAL | 游戏列表排序 |
| training_record | idx_child_id | child_id | NORMAL | 查询儿童训练记录 |
| training_record | idx_game_id | game_id | NORMAL | 查询游戏训练记录 |
| training_record | idx_created_at | created_at | NORMAL | 按时间查询训练 |
| training_record | idx_child_game | child_id, game_id | NORMAL | 联合查询 |
| training_record | idx_child_created | child_id, created_at | NORMAL | 联合查询 |
| parent_child | uk_user_child | user_id, child_id | UNIQUE | 唯一家长儿童关系 |
| parent_child | idx_child_id | child_id | NORMAL | 反向查询家长 |
| achievement | idx_achievement_code | achievement_code | UNIQUE | 成就代码查询 |
| achievement | idx_type | type | NORMAL | 按类型查询成就 |
| child_achievement | uk_child_achievement | child_id, achievement_id | UNIQUE | 唯一儿童成就 |
| membership | idx_tier | tier | NORMAL | 按等级查询套餐 |
| membership | idx_price | price | NORMAL | 按价格排序 |
| child_membership | uk_child_membership | child_id, membership_id | UNIQUE | 唯一儿童会员 |
| child_membership | idx_end_date | end_date | NORMAL | 查询即将过期 |
| order | idx_order_no | order_no | UNIQUE | 订单号查询 |
| order | idx_user_id | user_id | NORMAL | 查询用户订单 |
| order | idx_status | status | NORMAL | 按状态查询 |
| order | idx_pay_time | pay_time | NORMAL | 按时间查询 |
| focus_report | idx_child_id | child_id | NORMAL | 查询儿童报告 |
| focus_report | idx_report_type | report_type | NORMAL | 按类型查询报告 |
| focus_report | idx_period | period_start, period_end | NORMAL | 周期查询 |
| school | idx_code | code | UNIQUE | 学校代码查询 |
| school | idx_status | status | NORMAL | 按状态查询 |
| academy_article | idx_category_id | category_id | NORMAL | 按分类查询文章 |
| academy_article | idx_is_published | is_published | NORMAL | 查询已发布 |
| academy_question | idx_category_id | category_id | NORMAL | 按分类查询问题 |

---

## 7. 初始数据

### 7.1 游戏数据 (9款训练游戏)

```sql
INSERT INTO `game` (`game_code`, `game_name`, `description`, `category`, `difficulty_levels`, `training_focus`, `requires_vip`, `sort_order`) VALUES
('schulte', '舒尔特方格', '舒尔特方格是经典的视觉注意力训练工具，通过在方格中寻找并按顺序点击数字来训练视觉扫描能力和专注力', 'visual', 5, '视觉搜索,持续注意', 0, 1),
('audio_count', '听声辨数', '仔细听系统播放的声音序列，选择听到的数量', 'auditory', 5, '听觉注意,工作记忆', 0, 2),
('pattern_memory', '图案记忆', '记住并匹配不同的图案组合', 'memory', 5, '视觉记忆,模式识别', 0, 3),
('visual_tracking', '视觉追踪', '追踪移动的目标同时忽略干扰物', 'visual', 5, '视觉追踪,抗干扰', 0, 4),
('rhythm_tap', '节奏点击', '跟随音乐节拍进行点击训练', 'rhythm', 5, '节奏感知,反应控制', 1, 5),
('auditory_memory', '听觉记忆', '小侦探听口令，记住并复述数字序列', 'memory', 5, '听觉注意,序列记忆', 1, 6),
('maze_navigation', '迷宫寻路', '小熊找蜂蜜，通过迷宫到达目标', 'spatial', 5, '空间认知,执行功能', 1, 7),
('quick_sort', '快速分类', '物品整理小能手，快速将物品分类', 'cognitive', 5, '认知灵活,注意转移', 1, 8),
('target_tracking', '追踪目标', '追踪小星星，在干扰下保持注意力', 'visual', 5, '选择性注意,抗干扰', 1, 9);
```

### 7.2 会员套餐数据

```sql
INSERT INTO `membership` (`name`, `tier`, `price`, `duration_days`, `benefits`, `features`, `sort_order`) VALUES
('免费版', 'free', 0.00, 0, '["3个基础游戏", "每日5次训练", "基础报告"]', '["schulte", "audio_count", "pattern_memory"]', 1),
('年度会员', 'basic', 199.00, 365, '["全部9个游戏", "无限训练", "完整报告", "成就系统", "AI推荐"]', '["all_games", "unlimited", "full_reports", "achievements", "ai_recommend"]', 2),
('高级会员', 'premium', 399.00, 365, '["年度会员全部权益", "高级游戏皮肤", "深度分析报告", "优先客服"]', '["basic_features", "premium_skins", "deep_reports", "priority_support"]', 3);
```

### 7.3 成就数据

```sql
INSERT INTO `achievement` (`achievement_code`, `name`, `description`, `type`, `tier`, `requirement`, `reward_points`, `sort_order`) VALUES
('first_training', '初次训练', '完成第一次专注力训练', 'training', 'basic', '{"type": "training_count", "value": 1}', 10, 1),
('training_10', '训练新手', '累计完成10次训练', 'training', 'basic', '{"type": "training_count", "value": 10}', 50, 2),
('training_50', '训练达人', '累计完成50次训练', 'training', 'intermediate', '{"type": "training_count", "value": 50}', 200, 3),
('training_100', '训练大师', '累计完成100次训练', 'training', 'advanced', '{"type": "training_count", "value": 100}', 500, 4),
('perfect_score', '满分达成', '在任一游戏中获得满分', 'score', 'intermediate', '{"type": "perfect_score", "value": 1}', 100, 5),
('streak_3', '连续3天', '连续3天完成训练', 'streak', 'basic', '{"type": "streak", "value": 3}', 30, 6),
('streak_7', '连续一周', '连续7天完成训练', 'streak', 'intermediate', '{"type": "streak", "value": 7}', 100, 7),
('streak_30', '坚持一个月', '连续30天完成训练', 'streak', 'advanced', '{"type": "streak", "value": 30}', 500, 8),
('star_100', '百星闪耀', '累计获得100颗星星', 'milestone', 'intermediate', '{"type": "stars", "value": 100}', 200, 9),
('schulte_master', '舒尔特大师', '舒尔特方格达到9级难度', 'milestone', 'advanced', '{"type": "game_level", "game": "schulte", "level": 9}', 300, 10);
```

### 7.4 家长学院分类

```sql
INSERT INTO `academy_category` (`name`, `name_en`, `icon`, `sort_order`, `article_count`, `is_active`) VALUES
('注意力训练', 'Focus Training', '📚', 1, 0, 1),
('家长课堂', 'Parent School', '👨‍👩‍👧', 2, 0, 1),
('专家问答', 'Expert Q&A', '💬', 3, 0, 1),
('成长故事', 'Growth Stories', '⭐', 4, 0, 1);

INSERT INTO `academy_question_category` (`name`, `icon`, `sort_order`, `is_active`) VALUES
('注意力问题', '❓', 1, 1),
('学习方法', '📖', 2, 1),
('行为习惯', '🏃', 3, 1),
('情绪管理', '😊', 4, 1),
('其他问题', '📌', 5, 1);
```

---

## 8. ER关系图

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                              专注星球 数据库 ER 关系图                                      │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                          │
│  ┌──────────────┐         ┌──────────────────┐                                        │
│  │     user     │         │   parent_child    │                                        │
│  ├──────────────┤         ├──────────────────┤                                        │
│  │ id (PK)      │──┐ 1:N │ id (PK)          │                                        │
│  │ openid       │  ├──────│ user_id (FK)     │                                        │
│  │ nickname     │         │ child_id (FK)     │──┐ 1:N                                │
│  │ avatar       │         │ relation          │  │                                    │
│  │ phone        │         │ is_default        │  │                                    │
│  │ role         │         └──────────────────┘  │                                    │
│  │ status       │                              │                                    │
│  └──────────────┘                              │                                    │
│                                                │                                    │
│                                                ▼                                    │
│  ┌──────────────┐                              ┌──────────────────┐                  │
│  │    game      │                              │      child       │                  │
│  ├──────────────┤                              ├──────────────────┤                  │
│  │ id (PK)      │────────┐                     │ id (PK)          │                  │
│  │ game_code    │    1:N │                     │ name             │                  │
│  │ game_name    │        │                     │ age              │                  │
│  │ category     │        │                     │ gender           │                  │
│  │ requires_vip │        │                     │ age_group        │                  │
│  └──────────────┘        │                     │ level            │                  │
│                         │                     │ experience       │                  │
│                         │                     └──────┬───────────┘                  │
│                         │                            │ 1:N                            │
│                         │ 1:N                        ▼                                │
│                         │         ┌──────────────────────────────────────┐         │
│                         └─────────│       training_record                 │         │
│                                   ├──────────────────────────────────────┤         │
│                                   │ id (PK)                              │         │
│                                   │ child_id (FK) ──────────┐            │         │
│                                   │ game_id (FK) ──────────┘            │         │
│                                   │ score, accuracy, focus_score        │         │
│                                   │ duration_seconds                    │         │
│                                   │ difficulty_level                    │         │
│                                   │ game_config (JSON)                  │         │
│                                   │ result_data (JSON)                  │         │
│                                   └──────────────────────────────────────┘         │
│                                                                                          │
│  ┌──────────────────┐                        ┌──────────────────┐                    │
│  │    achievement    │                        │    focus_report  │                    │
│  ├──────────────────┤    1:N                 ├──────────────────┤                    │
│  │ id (PK)          │───────────────────────│ child_id (FK)     │                    │
│  │ achievement_code │                       │ report_type      │                    │
│  │ name             │                       │ dimensions (JSON)│                    │
│  │ type             │                       │ overall_score    │                    │
│  │ tier             │                       │ training_count   │                    │
│  │ requirement (JSON)                     │ total_duration   │                    │
│  └──────────────────┘                       └──────────────────┘                    │
│           │                                                                  │
│           │ 1:N                                                              │
│           ▼                                                                  │
│  ┌─────────────────────────┐                                               │
│  │   child_achievement     │                                               │
│  ├─────────────────────────┤                                               │
│  │ id (PK)                 │                                               │
│  │ child_id (FK) ──────────┘                                                │
│  │ achievement_id (FK)                                                    │
│  │ progress, unlocked_at                                                   │
│  └─────────────────────────┘                                               │
│                                                                                          │
│  ┌──────────────────┐         ┌──────────────────┐    ┌──────────────────┐   │
│  │   membership      │         │ child_membership │    │      order       │   │
│  ├──────────────────┤    1:N  ├──────────────────┤    ├──────────────────┤   │
│  │ id (PK)          │─────────│ child_id (FK)    │────│ order_no (UK)    │   │
│  │ tier             │         │ membership_id(FK)│    │ user_id (FK)     │   │
│  │ price            │         │ start_date       │    │ child_id (FK)    │   │
│  │ benefits (JSON)  │         │ end_date         │    │ membership_id(FK)│   │
│  │ features (JSON)  │         │ status           │    │ amount           │   │
│  └──────────────────┘         └──────────────────┘    │ status           │   │
│                                                       └──────────────────┘   │
│                                                                                          │
│  ┌───────────────────────────────────────────────────────────────────────────────┐   │
│  │                               学校管理模块                                       │   │
│  ├───────────────────────────────────────────────────────────────────────────────┤   │
│  │                                                                               │   │
│  │   ┌──────────────┐         ┌──────────────┐         ┌──────────────┐         │   │
│  │   │    school    │    1:N │    teacher    │    N:1  │     user     │         │   │
│  │   ├──────────────┤─────────├──────────────┤─────────├──────────────┤         │   │
│  │   │ id (PK)      │         │ id (PK)      │         │ id (PK)       │         │   │
│  │   │ name         │         │ user_id (FK) │◄────────│ openid        │         │   │
│  │   │ code (UK)    │         │ school_id(FK)│         │ role=teacher │         │   │
│  │   │ status       │         │ name         │         └──────────────┘         │   │
│  │   └──────┬───────┘         │ role         │                                  │   │
│  │          │ 1:N             └──────────────┘                                  │   │
│  │          ▼                                                                      │   │
│  │   ┌──────────────┐         ┌──────────────┐                                  │   │
│  │   │     class    │    1:N │ class_student │    N:1                          │   │
│  │   ├──────────────┤─────────├──────────────┤◄───────────────────────────────┤   │
│  │   │ id (PK)      │         │ id (PK)       │                                │   │
│  │   │ school_id(FK)│         │ class_id (FK) │                                │   │
│  │   │ name         │         │ child_id (FK) ────────────────────────────────┘   │
│  │   │ grade        │         │ status        │                                  │   │
│  │   └──────────────┘         └──────────────┘                                  │   │
│  │                                                                               │   │
│  └───────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                          │
│  ┌───────────────────────────────────────────────────────────────────────────────┐   │
│  │                               家长学院模块                                       │   │
│  ├───────────────────────────────────────────────────────────────────────────────┤   │
│  │                                                                               │   │
│  │   ┌──────────────────┐         ┌──────────────────┐                          │   │
│  │   │ academy_category │    1:N │ academy_article   │                          │   │
│  │   ├──────────────────┤─────────├──────────────────┤                          │   │
│  │   │ id (PK)          │         │ id (PK)          │                          │   │
│  │   │ name             │         │ category_id (FK) │                          │   │
│  │   │ sort_order       │         │ title            │                          │   │
│  │   └──────────────────┘         │ content          │                          │   │
│  │                                 │ read_count       │                          │   │
│  │   ┌──────────────────┐         └──────────────────┘                          │   │
│  │   │academy_question_ │    1:N │ academy_question  │    N:1                   │   │
│  │   │     category      │─────────├──────────────────┤────────────────────────┘   │
│  │   ├──────────────────┤         │ id (PK)          │                          │   │
│  │   │ id (PK)          │         │ user_id (FK)    │                          │   │
│  │   │ name             │         │ category_id (FK)│                          │   │
│  │   └──────────────────┘         │ title            │                          │   │
│  │                                 │ content          │                          │   │
│  │                                 │ status           │                          │   │
│  │                                 └───────┬──────────┘                          │   │
│  │                                         │ 1:N                                 │   │
│  │                                         ▼                                     │   │
│  │                                 ┌──────────────────┐                          │   │
│  │                                 │  academy_answer  │                          │   │
│  │                                 ├──────────────────┤                          │   │
│  │                                 │ id (PK)         │                          │   │
│  │                                 │ question_id(FK) │                          │   │
│  │                                 │ user_id (FK)    │                          │   │
│  │                                 │ content         │                          │   │
│  │                                 │ is_expert       │                          │   │
│  │                                 │ is_official     │                          │   │
│  │                                 └──────────────────┘                          │   │
│  │                                                                               │   │
│  └───────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                          │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 附录

### A. 数据库初始化SQL位置

- **Docker初始化**: `docker/mysql/init/init.sql`
- **Server初始化**: `packages/server/sql/init.sql`

### B. 数据库配置

```ini
# MySQL 8.0 优化配置
[mysqld]
max_connections = 500
wait_timeout = 28800
innodb_buffer_pool_size = 2G
innodb_log_file_size = 1G
```

### C. Redis缓存设计

| 用途 | Key Pattern | TTL |
|------|-------------|-----|
| JWT会话 | session:{user_id} | 7天 |
| 频率限制 | rate:{api}:{ip} | 1分钟 |
| 排行榜 | leaderboard:{game_id} | 实时更新 |

---

*本文档根据代码实现最新更新 V2.0 - 2025年5月*