# 儿童专注力训练 - 游戏02听声辨数UI设计规范

## 文档信息

| 项目 | 内容 |
|------|------|
| 游戏编号 | 游戏02 |
| 游戏名称 | 听声辨数 (Audio Number) |
| 文档类型 | UI设计规范 |
| 版本 | V1.0 |

---

## 目录

1. [色彩系统](#1-色彩系统)
2. [字体系统](#2-字体系统)
3. [图标与图片资源](#3-图标与图片资源)
4. [动画设计规范](#4-动画设计规范)
5. [页面详细布局](#5-页面详细布局)
6. [交互细节](#6-交互细节)

---

## 1. 色彩系统

### 1.1 游戏专属色彩

```css
:root {
  /* 听声辨数游戏色彩 */
  
  /* 声音播放状态 */
  --sound-playing-bg: #6EC6FF;
  --sound-playing-glow: rgba(110, 198, 255, 0.5);
  
  /* 选项按钮 */
  --option-default-bg: #FFFFFF;
  --option-default-border: #E0E0E0;
  --option-default-text: #333333;
  
  --option-selected-bg: #6C63FF;
  --option-selected-border: #6C63FF;
  --option-selected-text: #FFFFFF;
  
  --option-correct-bg: #6BCB77;
  --option-correct-border: #6BCB77;
  --option-correct-text: #FFFFFF;
  
  --option-wrong-bg: #FF8A80;
  --option-wrong-border: #FF8A80;
  --option-wrong-text: #FFFFFF;
  
  --option-disabled-bg: #F5F5F5;
  --option-disabled-border: #E0E0E0;
  --option-disabled-text: #BDBDBD;
  
  /* 背景 */
  --game-bg-gradient-start: #6EC6FF;
  --game-bg-gradient-end: #8B85FF;
  
  /* 播放指示器 */
  --indicator-active: #6C63FF;
  --indicator-inactive: #E0E0E0;
  --indicator-playing: #FFD93D;
}
```

### 1.2 色彩使用规范

| 场景 | 背景色 | 文字色 | 特殊效果 |
|------|--------|--------|----------|
| 播放区域 | #6EC6FF | #FFFFFF | 呼吸动画 |
| 选项默认 | #FFFFFF | #333333 | 阴影 |
| 选项选中 | #6C63FF | #FFFFFF | 阴影加深 |
| 选项正确 | #6BCB77 | #FFFFFF | 成功动画 |
| 选项错误 | #FF8A80 | #FFFFFF | 抖动动画 |
| 背景 | 渐变蓝紫 | - | 无 |

---

## 2. 字体系统

```css
:root {
  /* 听声辨数字体 */
  --font-family-title: 'PingFang SC', 'Helvetica Neue', sans-serif;
  --font-family-body: 'PingFang SC', 'Helvetica Neue', sans-serif;
  --font-family-number: 'DIN Alternate', 'Helvetica Neue', sans-serif;
  
  /* 字号 */
  --font-size-title: 28sp;
  --font-size-option: 28sp;     /* 选项数字 */
  --font-size-target: 20sp;     /* 目标提示 */
  --font-size-timer: 24sp;
  --font-size-label: 16sp;
  --font-size-hint: 14sp;
}
```

---

## 3. 图标与图片资源

| 图标名称 | 用途 | 尺寸 |
|----------|------|------|
| ic_audio_ear | 耳朵图标 | 128×128dp |
| ic_audio_speaker | 播放图标 | 48×48dp |
| ic_audio_wave | 声波动画 | 64×64dp |
| ic_replay | 重播图标 | 24×24dp |
| ic_check_circle | 正确 | 24×24dp |
| ic_cancel | 错误 | 24×24dp |

---

## 4. 动画设计规范

### 4.1 声音播放动画

```typescript
const SOUND_PLAY_ANIMATIONS = {
  // 播放按钮动画
  playButton: {
    idle: {
      scale: 1,
      opacity: 1,
    },
    playing: {
      scale: [1, 1.1, 1],
      opacity: [1, 0.8, 1],
      duration: 600,
      repeat: 'infinite',
    },
  },
  
  // 声波动画
  waveAnimation: {
    active: {
      scale: [1, 1.5, 2],
      opacity: [1, 0.5, 0],
      duration: 800,
      repeat: 'infinite',
    },
  },
  
  // 播放指示点动画
  dotAnimation: {
    active: {
      scale: [0.5, 1.2, 0.5],
      duration: 400,
      repeat: 'infinite',
    },
  },
};
```

### 4.2 选项动画

```typescript
const OPTION_ANIMATIONS = {
  // 选项出现
  appear: {
    scale: '0.8 → 1',
    opacity: '0 → 1',
    stagger: 100,  // 依次延迟
    duration: 300,
    easing: 'spring',
  },
  
  // 选择反馈
  select: {
    scale: '1 → 0.95 → 1',
    duration: 150,
  },
  
  // 正确反馈
  correct: {
    scale: '1 → 1.1 → 1',
    backgroundColor: '#6BCB77',
    duration: 300,
    haptic: 'success',
  },
  
  // 错误反馈
  wrong: {
    backgroundColor: '#FF8A80',
    shake: true,
    shakeOffset: 8,
    shakeCount: 4,
    duration: 400,
    haptic: 'error',
  },
};
```

---

## 5. 页面详细布局

### 5.1 游戏页布局

```
┌─────────────────────────────────────────────────────────────┐
│ ← 返回              ⏱️ 02:35              ⏸️暂停          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                 第 3 题 / 共 10 题                         │
│                 ████████░░░░░░░░░░░                       │
│                                                             │
│          ┌─────────────────────────────────┐              │
│          │                                 │              │
│          │                                 │              │
│          │      🔊 播放中...              │              │
│          │                                 │              │
│          │      ● ● ● ○ ○  (5个)          │              │
│          │    (指示点)                      │              │
│          │                                 │              │
│          │      [🔄 重播 3/3]              │              │
│          │                                 │              │
│          └─────────────────────────────────┘              │
│                                                             │
│          "数一数有多少声音，选择正确答案"                  │
│                                                             │
│          ┌─────────────────────────────────────┐          │
│          │                                     │          │
│          │     [ 3 ]  [ 4 ]  [ 5 ]           │          │
│          │                                     │          │
│          │          [ 6 ]  [ 7 ]              │          │
│          │                                     │          │
│          └─────────────────────────────────────┘          │
│                                                             │
│              ✅ 正确: 2    ❌ 错误: 0                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘

布局参数：
- 页面边距: 24dp
- 进度条: 居中，宽100%，高4dp，圆角2dp
- 播放区域: 居中，宽度100%，高度200dp，圆角24dp
- 播放区域背景: #6EC6FF，柔和阴影
- 播放图标: 48dp，距顶部24dp
- 指示点: 12dp，间距8dp
- 重播按钮: 距底部16dp
- 选项区域: 距播放区域24dp
- 选项按钮: 宽度80dp，高度80dp，圆角16dp
- 选项按钮间距: 12dp
- 统计栏: 距底部32dp
```

### 5.2 选项布局

```typescript
// 选项布局配置
const OPTION_LAYOUTS = {
  3: {
    columns: 3,
    rows: 1,
    layout: [
      [0, 1, 2],
    ],
  },
  4: {
    columns: 2,
    rows: 2,
    layout: [
      [0, 1],
      [2, 3],
    ],
  },
  5: {
    columns: 3,
    rows: 2,
    layout: [
      [0, 1, 2],
      [3, 4],
    ],
  },
  6: {
    columns: 3,
    rows: 2,
    layout: [
      [0, 1, 2],
      [3, 4, 5],
    ],
  },
};

// 按钮尺寸
const OPTION_SIZES = {
  button: {
    width: 72,
    height: 72,
    fontSize: 28,
    borderRadius: 16,
  },
};
```

---

## 6. 交互细节

### 6.1 播放交互

```typescript
const AUDIO_INTERACTION = {
  // 播放按钮
  playButton: {
    touchArea: 56,
    tapAnimation: {
      scale: 0.9,
      duration: 100,
    },
  },
  
  // 重播按钮
  replayButton: {
    touchArea: 48,
    tapAnimation: {
      scale: 0.95,
      duration: 100,
    },
    // 禁用状态显示
    disabledText: '不可重播',
  },
  
  // 选项按钮
  optionButton: {
    touchArea: 80,
    tapAnimation: {
      scale: 0.95,
      duration: 80,
    },
    debounceDelay: 200,  // 防止重复点击
  },
};
```

### 6.2 音效触发

```typescript
const AUDIO_SFX = {
  // 播放提示音
  onPlaySound: 'sfx_beep.mp3',
  onSoundEnd: 'sfx_tick.mp3',
  
  // 选择反馈
  onCorrect: 'sfx_correct.mp3',
  onWrong: 'sfx_wrong.mp3',
  
  // 游戏完成
  onGameComplete: 'sfx_complete.mp3',
};
```

### 6.3 震动反馈

```typescript
const AUDIO_HAPTIC = {
  onSoundPlay: { type: 'impactLight', duration: 10 },
  onCorrect: { type: 'notificationSuccess' },
  onWrong: { type: 'notificationError' },
};
```
